---
phase: 08-post-event-registration-closure-activate-completed-mode-back
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config.ts
  - backend/src/lib/eventClosure.ts
  - backend/src/lib/eventClosure.test.ts
  - backend/src/routes/register.ts
  - backend/src/routes/fundraiser.ts
  - backend/src/routes/donors.ts
  - backend/src/routes/record-donation.ts
  - backend/src/routes/fundraiser.closure.test.ts
  - backend/src/routes/donors.closure.test.ts
  - backend/src/routes/confirm-payment.closure.test.ts
  - backend/src/routes/record-donation.closure.test.ts
  - backend/.env.example
autonomous: true
requirements:
  - API-02
  - API-03
  - API-07
  - REGA-06
must_haves:
  truths:
    - "D-05: POST /api/register returns 403 REGISTRATION_CLOSED when EVENT_STATUS=completed"
    - "D-07: POST /api/donors and POST /api/donation/:slug return 403 when completed"
    - "D-08: POST /api/fundraiser, PUT /api/fundraiser/:slug, POST /api/fundraiser/register return 403 when completed"
    - "D-06: POST /api/register/confirm-payment still succeeds when EVENT_STATUS=completed"
    - "D-10: config.eventStatus reads EVENT_STATUS env var (not calendar date)"
    - "D-11: GET /api/fundraiser/:slug and GET /api/progress remain available when completed"
  artifacts:
    - path: backend/src/lib/eventClosure.ts
      provides: "Shared isEventCompleted() and registrationClosedResponse() helpers"
      exports: ["isEventCompleted", "registrationClosedResponse"]
    - path: backend/src/config.ts
      provides: "eventStatus from EVENT_STATUS env var"
      contains: "eventStatus"
    - path: backend/src/routes/register.ts
      provides: "Closure guard on POST /"
      pattern: "isEventCompleted"
    - path: backend/src/routes/fundraiser.ts
      provides: "Closure guards on POST /, PUT /:slug, POST /register"
      pattern: "isEventCompleted"
  key_links:
    - from: backend/src/routes/register.ts
      to: backend/src/lib/eventClosure.ts
      via: "import isEventCompleted, registrationClosedResponse"
      pattern: "registrationClosedResponse"
    - from: backend/src/config.ts
      to: backend/src/lib/eventClosure.ts
      via: "config.eventStatus read"
      pattern: "config\\.eventStatus"
---

<objective>
Add backend write guards keyed on `EVENT_STATUS=completed`, blocking new registrations, fundraiser writes, and donor wall entries while keeping `confirm-payment` and read endpoints open.

Purpose: Enforce closure server-side per D-05, D-07, D-08, D-10, D-11 even if frontend is bypassed.
Output: `eventClosure.ts` helper, `config.eventStatus`, guarded write routes, closure unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-RESEARCH.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-PATTERNS.md

<interfaces>
From backend/src/config.ts (add alongside existing frozen fields):
```typescript
export const config = Object.freeze({
  // ...existing fields...
  eventStatus: (process.env.EVENT_STATUS === "completed"
    ? "completed"
    : "active") as "active" | "completed",
});
```

From backend/src/types.ts:
```typescript
export interface ValidationError {
  field: string;
  message: string;
  code?: string;
}
export type ApiResponse<T> =
  | { success: true; data: T }
  | { success: false; errors: ValidationError[] };
```

From backend/src/routes/fundraiser.ts (403 error pattern to mirror):
```typescript
return c.json(
  { success: false, errors: [{ field: "authorization", message: "Edit token required", code: "VALIDATION_AUTH_REQUIRED" }] } satisfies ApiResponse<never>,
  403,
);
```

Route mount order in backend/src/index.ts (confirm-payment is separate — do NOT guard it):
```typescript
app.route("/api/register/confirm-payment", confirmPaymentRoute);
app.route("/api/register/by-token", lookupRoute);
app.route("/api/register", registerRoute);
```

Guarded write handlers (insert guard as first line inside handler):
- register.ts: `registerRoute.post("/", ...)` line ~94
- fundraiser.ts: `post("/")` line ~31, `put("/:slug")` line ~120, `post("/register")` line ~208
- donors.ts: `donorsRoute.post("/", ...)` line ~9
- record-donation.ts: `recordDonationRoute.post("/:slug", ...)` line ~9
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eventClosure helper, config.eventStatus, env docs</name>
  <files>backend/src/lib/eventClosure.ts, backend/src/lib/eventClosure.test.ts, backend/src/config.ts, backend/.env.example</files>
  <read_first>backend/src/config.ts, backend/src/types.ts, backend/src/routes/fundraiser.ts, backend/.env.example, backend/src/lib/whydonateOrders.test.ts</read_first>
  <behavior>
    - isEventCompleted() returns true when config.eventStatus === "completed"
    - isEventCompleted() returns false when config.eventStatus === "active" or EVENT_STATUS unset
    - registrationClosedResponse returns HTTP 403 with code REGISTRATION_CLOSED, field _global, message "Registration is closed"
  </behavior>
  <action>
    Create backend/src/lib/eventClosure.ts exporting isEventCompleted() and registrationClosedResponse(c) per D-10. Mirror frontend useEventStatus.ts ternary: default "active" when EVENT_STATUS unset (safe for local dev and pre-closure production).

    Add eventStatus to frozen config in backend/src/config.ts using process.env.EVENT_STATUS === "completed" ? "completed" : "active".

    Document EVENT_STATUS=active in backend/.env.example after GOAL_EUR block with comment explaining "active" (default) vs "completed" (blocks writes). Add note in the existing GitHub Actions repository variables comment block that vars.EVENT_STATUS should be set to completed only at closure time.

    Write backend/src/lib/eventClosure.test.ts first (RED), then implement (GREEN). Mock config via vi.mock("../config.js", () => ({ config: { eventStatus: "completed" } })) for completed-case tests.
  </action>
  <verify>
    <automated>npx vitest run backend/src/lib/eventClosure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/lib/eventClosure.ts exports isEventCompleted and registrationClosedResponse
    - config.eventStatus is typed "active" | "completed" and defaults to "active"
    - backend/.env.example documents EVENT_STATUS with active default
    - All eventClosure.test.ts cases pass
  </acceptance_criteria>
  <done>Shared closure helper exists, config reads EVENT_STATUS, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Apply closure guards to all write routes</name>
  <files>backend/src/routes/register.ts, backend/src/routes/fundraiser.ts, backend/src/routes/donors.ts, backend/src/routes/record-donation.ts</files>
  <read_first>backend/src/routes/register.ts, backend/src/routes/fundraiser.ts, backend/src/routes/donors.ts, backend/src/routes/record-donation.ts, backend/src/routes/confirm-payment.ts, backend/src/index.ts</read_first>
  <action>
    Per D-05, D-07, D-08: at the top of each blocked write handler (before body parsing or Sheets I/O), add:
    if (isEventCompleted()) return registrationClosedResponse(c);

    Guard these handlers only:
    - register.ts: POST /
    - fundraiser.ts: POST /, PUT /:slug, POST /register
    - donors.ts: POST /
    - record-donation.ts: POST /:slug

    Per D-06 and D-11: do NOT add guards to confirm-payment.ts, lookup.ts (by-token), progress.ts GET, or fundraiser GET /:slug. Import isEventCompleted and registrationClosedResponse from ../lib/eventClosure.js in each guarded file.

    Do not change validation logic, email sending, or Sheets writes below the guard.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 6 write handlers have closure guard as first statement
    - confirm-payment.ts has zero isEventCompleted references
    - fundraiser GET /:slug handler has zero closure guard
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All write routes guarded; confirm-payment and read routes untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route-level closure tests</name>
  <files>backend/src/routes/fundraiser.closure.test.ts, backend/src/routes/donors.closure.test.ts, backend/src/routes/confirm-payment.closure.test.ts, backend/src/routes/record-donation.closure.test.ts</files>
  <read_first>backend/src/routes/fundraiser.ts, backend/src/routes/donors.ts, backend/src/routes/confirm-payment.ts, backend/src/lib/eventClosure.test.ts, backend/src/index.ts</read_first>
  <behavior>
    - POST /api/register returns 403 with REGISTRATION_CLOSED when eventStatus is completed (test via register route or shared helper integration)
    - POST /api/fundraiser returns 403 REGISTRATION_CLOSED when completed
    - PUT /api/fundraiser/:slug returns 403 when completed
    - POST /api/donors returns 403 when completed
    - POST /api/donation/:slug returns 403 REGISTRATION_CLOSED when completed
    - POST /api/register/confirm-payment does NOT return REGISTRATION_CLOSED when completed (handler proceeds past guard or is unguarded)
  </behavior>
  <action>
    Create route-level closure tests using vi.mock on ../config.js to set eventStatus: "completed". Use Hono app.request() against a minimal app mounting the relevant route, or invoke handlers directly with mock Context.

  fundraiser.closure.test.ts: assert POST /, PUT /test-slug, POST /register all return 403 + errors[0].code === "REGISTRATION_CLOSED" when completed.

  donors.closure.test.ts: assert POST / returns 403 REGISTRATION_CLOSED when completed.

  record-donation.closure.test.ts: assert POST /:slug returns 403 REGISTRATION_CLOSED when completed (same guard as donors per D-07).

  confirm-payment.closure.test.ts: assert confirm-payment route does not return REGISTRATION_CLOSED when completed (mock SheetsService to avoid live Sheets; test may assert handler is reachable or returns validation error instead of closure code).

  Also add register closure test in eventClosure.test.ts or a register.closure.test.ts if not covered — POST /api/register must return 403 when completed per API-02.
  </action>
  <verify>
    <automated>npx vitest run backend/src/routes/fundraiser.closure.test.ts backend/src/routes/donors.closure.test.ts backend/src/routes/record-donation.closure.test.ts backend/src/routes/confirm-payment.closure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Blocked routes return 403 with code REGISTRATION_CLOSED when config.eventStatus is "completed"
    - confirm-payment test proves closure code is absent when completed
    - All three closure test files pass
    - npm test passes with no regressions
  </acceptance_criteria>
  <done>Automated tests prove write guards active and confirm-payment exempt per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API write endpoints | Untrusted clients can POST directly, bypassing closed frontend UI |
| Ops → EVENT_STATUS env | Only deploy pipeline / Cloud Run should set completed status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | POST /api/register, /api/fundraiser, /api/donors, /api/donation/:slug | mitigate | isEventCompleted() guard returns 403 REGISTRATION_CLOSED before any Sheets write (D-05, D-07, D-08) |
| T-8-02 | Elevation | POST /api/register/confirm-payment | mitigate | Explicitly exclude confirm-payment from guards; only existing pending rows can complete payment (D-06) |
| T-8-03 | Information Disclosure | GET /api/progress post-closure | accept | Live progress remains for ops comparison; public site uses frozen finalStats at build time |
| T-8-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan; slopcheck N/A |

</threat_model>

<verification>
- `npx vitest run backend/src/lib/eventClosure.test.ts backend/src/routes/*.closure.test.ts`
- `cd backend && npx tsc --noEmit`
- `npm test` (full suite, no regressions)
</verification>

<success_criteria>
1. All write endpoints return 403 REGISTRATION_CLOSED when EVENT_STATUS=completed
2. confirm-payment and read endpoints work when completed
3. config.eventStatus defaults to active when env unset
4. All closure tests pass
</success_criteria>

<output>
Create `.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-01-SUMMARY.md` when done
</output>
