---
phase: 08-post-event-registration-closure-activate-completed-mode-back
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/events/2026-run-for-ukraine/register/page.tsx
  - src/__tests__/register-page-completed.test.tsx
autonomous: true
requirements:
  - REGA-06
  - EVNT-02
must_haves:
  truths:
    - "D-06: Register page shows RegisterClient/ConfirmationPanel when completed and ?token= is present"
    - "D-09: Register page shows closed banner when completed and no ?token= param (no redirect to landing)"
    - "Fundraise page remains fully closed in completed mode (no token exception)"
  artifacts:
    - path: src/app/events/2026-run-for-ukraine/register/page.tsx
      provides: "Token exception for late payment flow"
      pattern: "showClosedBanner"
    - path: src/__tests__/register-page-completed.test.tsx
      provides: "Component tests for D-06 token exception"
      min_lines: 40
  key_links:
    - from: src/app/events/2026-run-for-ukraine/register/page.tsx
      to: src/components/ui/RegisterClient.tsx
      via: "renders RegisterClient when hasToken && isCompleted"
      pattern: "RegisterClient"
    - from: RegisterClient
      to: /api/register/confirm-payment
      via: "ConfirmationPanel payment flow"
      pattern: "confirm-payment"
---

<objective>
Fix register page completed-mode gate so pending registrants with `?token=` deep links can still complete payment after closure.

Purpose: Satisfy D-06 — register UI closed for new visitors but payment confirmation stays reachable.
Output: Token-aware closed banner logic + component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-RESEARCH.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-PATTERNS.md

<interfaces>
From src/app/events/2026-run-for-ukraine/register/page.tsx (current — blocks entire page):
```tsx
const isCompleted = useEventStatus() === "completed";
// ...
{isCompleted ? (
  <div className={styles.closedBanner}>...</div>
) : (
  <RegisterClient onStepChange={setStep} />
)}
```

From src/hooks/useEventStatus.ts:
```typescript
export function getEventStatus(): EventStatus {
  return process.env.NEXT_PUBLIC_EVENT_STATUS === "completed"
    ? "completed"
    : "active";
}
```

From src/__tests__/RegisterClient.test.tsx (token URL pattern):
```typescript
window.history.replaceState({}, "", "?token=NEW_TOKEN");
render(<RegisterClient />);
```

Target fix per D-06:
```tsx
import { useSearchParams } from "next/navigation";
const searchParams = useSearchParams();
const hasToken = Boolean(searchParams.get("token"));
const showClosedBanner = isCompleted && !hasToken;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register page token exception + component tests (TDD)</name>
  <files>src/app/events/2026-run-for-ukraine/register/page.tsx, src/__tests__/register-page-completed.test.tsx</files>
  <read_first>src/app/events/2026-run-for-ukraine/register/page.tsx, src/app/events/2026-run-for-ukraine/fundraise/page.tsx, src/hooks/useEventStatus.ts, src/components/ui/RegisterClient.tsx, src/__tests__/RegisterClient.test.tsx</read_first>
  <behavior>
    - When NEXT_PUBLIC_EVENT_STATUS=completed and URL has no token: closed banner visible, RegisterClient not rendered
    - When NEXT_PUBLIC_EVENT_STATUS=completed and URL has ?token=XYZ: RegisterClient rendered, closed banner not visible
    - When status is active: RegisterClient always rendered regardless of token
  </behavior>
  <action>
    RED: Create src/__tests__/register-page-completed.test.tsx first following RegisterClient.test.tsx setup: vi.mock useEventStatus, vi.mock useSearchParams from next/navigation, vi.mock LocaleProvider/t if needed. Mock RegisterClient with testid="register-client". Test cases: (1) completed + no token → closed heading visible, register-client absent; (2) completed + token=abc → register-client present, closed.registrationClosed absent; (3) active + no token → register-client present.

    GREEN: Per D-06 and D-09, update register/page.tsx — import useSearchParams from next/navigation, compute hasToken = Boolean(searchParams.get("token")), replace isCompleted ternary with showClosedBanner = isCompleted && !hasToken.

    Keep existing closed banner markup unchanged (locale keys: closed.registrationClosed, closed.eventCompleted, closed.seeResults). Do NOT redirect to landing page. Do NOT add token exception to fundraise/page.tsx.

    If useSearchParams requires Suspense boundary per Next.js 16 convention, wrap only the token-reading portion or add Suspense fallback consistent with existing app patterns.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/register-page-completed.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - register/page.tsx uses showClosedBanner = isCompleted && !hasToken
    - Closed banner hidden when ?token= present and status completed
    - RegisterClient visible when ?token= present and status completed
    - fundraise/page.tsx unchanged
    - register-page-completed.test.tsx has at least 3 cases and all pass
    - npm test passes with no regressions
  </acceptance_criteria>
  <done>D-06 satisfied: late payment deep links work in completed mode with automated test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → register page | URL token param controls whether closed banner or payment flow shows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Spoofing | ?token= without valid registration | accept | Invalid tokens fail at API lookup; UI only gates visibility, not authorization |
| T-8-05 | Tampering | User removes token to re-open registration form | mitigate | Backend POST /api/register still blocked when completed (Plan 01); form submit would fail |
| T-8-SC | Tampering | npm installs | mitigate | No new packages |

</threat_model>

<verification>
- `npx vitest run src/__tests__/register-page-completed.test.tsx`
- `npm test`
- `npm run typecheck`
</verification>

<success_criteria>
1. Completed mode + no token shows closed banner only
2. Completed mode + ?token= shows payment confirmation flow
3. Active mode unchanged
4. Component tests pass
</success_criteria>

<output>
Create `.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-02-SUMMARY.md` when done
</output>
