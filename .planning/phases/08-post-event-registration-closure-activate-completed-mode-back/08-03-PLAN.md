---
phase: 08-post-event-registration-closure-activate-completed-mode-back
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - backend/src/snapshot-final-stats.ts
  - backend/src/snapshot-final-stats.test.ts
  - backend/package.json
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - EVNT-02
  - POST-02
must_haves:
  truths:
    - "D-12: npm run snapshot-final-stats reads reconciled Sheets data via getProgress() (not pre-reconciliation API)"
    - "D-13: Script outputs reviewable stdout JSON before event.ts commit"
    - "D-14: chargingStations only via --charging-stations CLI flag, never auto-calculated"
    - "D-17: galleryFolderId and EventGallery not activated in this phase"
    - "D-18: Placeholder gallery remains hidden; not a closure blocker"
  artifacts:
    - path: backend/src/snapshot-final-stats.ts
      provides: "Ops CLI to freeze reconciled totals"
      min_lines: 40
    - path: backend/package.json
      provides: "snapshot-final-stats npm script alias"
      contains: "snapshot-final-stats"
    - path: backend/src/snapshot-final-stats.test.ts
      provides: "Unit tests with mocked SheetsService"
      min_lines: 30
  key_links:
    - from: backend/src/snapshot-final-stats.ts
      to: backend/src/services/sheets.ts
      via: "SheetsService.getProgress()"
      pattern: "getProgress"
    - from: src/data/event.ts
      to: backend/src/snapshot-final-stats.ts
      via: "ops copies raised/participants/donors into postEvent.finalStats"
      pattern: "finalStats"
---

<objective>
Create an ops snapshot script that reads reconciled totals from Google Sheets and outputs reviewable JSON for populating `event.ts` finalStats before closure deploy.

Purpose: Satisfy D-12, D-13, D-14 — freeze numbers only after reconciliation, human review before commit.
Output: `snapshot-final-stats.ts`, npm script, unit tests. Does NOT auto-deploy or flip env vars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-RESEARCH.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-PATTERNS.md

<interfaces>
From backend/src/services/sheets.ts (getProgress return shape):
```typescript
async getProgress(): Promise<{
  totalRaisedEur: number;
  participantCount: number;
  donorCount: number;
}>;
```

From src/data/event.ts (snapshot target — ops updates manually):
```typescript
finalStats: {
  raised: number;
  participants: number;
  donors: number;
  chargingStations: number;
};
```

From backend/src/audit-whydonate-records.ts (ops script pattern):
```typescript
async function main(): Promise<void> { /* ... */ }
main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

From backend/package.json:
```json
"audit-whydonate-records": "node --env-file=.env --import tsx src/audit-whydonate-records.ts"
```

Pre-closure gate (ops, not automated): run `npm run audit-whydonate-records` until clean per D-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snapshot-final-stats ops script</name>
  <files>backend/src/snapshot-final-stats.ts, backend/package.json</files>
  <read_first>backend/src/audit-whydonate-records.ts, backend/src/routes/progress.ts, backend/src/services/sheets.ts, backend/package.json, src/data/event.ts</read_first>
  <behavior>
    - Default run prints JSON to stdout: { raised, participants, donors } mapped from getProgress()
    - --charging-stations N adds chargingStations: N to output (D-14 manual only)
    - --apply patches only finalStats.raised, finalStats.participants, finalStats.donors in src/data/event.ts (optional; default stdout-only per D-13)
    - Sheets failure exits code 1
  </behavior>
  <action>
    Per D-12, D-13, D-14: create backend/src/snapshot-final-stats.ts following audit-whydonate-records.ts conventions.

    JSDoc header with Usage:
      npm run snapshot-final-stats
      npm run snapshot-final-stats -- --charging-stations 12
      npm run snapshot-final-stats -- --apply

    main(): instantiate SheetsService, call getProgress(), map:
      raised = totalRaisedEur
      participants = participantCount (all registration rows per RESEARCH Pitfall 4 — document in script header comment)
      donors = donorCount

    Default: console.log(JSON.stringify(snapshot, null, 2)) to stdout only.

    --charging-stations N: parse integer from argv; include chargingStations in output JSON only when flag present. Never derive from raised total.

    --apply (optional): read ../../src/data/event.ts relative to script, regex-replace finalStats numeric fields only. Do NOT modify thankYouMessage, impactStatement, or galleryFolderId. Print warning that copy/chargingStations require manual edit per D-16.

    Add npm script: "snapshot-final-stats": "node --env-file=.env --import tsx src/snapshot-final-stats.ts"

    Per D-17/D-18: do not touch galleryFolderId or EventGallery.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/snapshot-final-stats.ts exists with main().catch exit(1) pattern
    - backend/package.json has snapshot-final-stats script
    - Script uses SheetsService.getProgress(), not ad-hoc range parsing
    - Default mode prints JSON to stdout without writing files
    - chargingStations never auto-calculated from raised
  </acceptance_criteria>
  <done>Ops can run npm run snapshot-final-stats after reconciliation audit is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot script unit tests</name>
  <files>backend/src/snapshot-final-stats.test.ts</files>
  <read_first>backend/src/snapshot-final-stats.ts, backend/src/lib/whydonateOrders.test.ts</read_first>
  <behavior>
    - Mock getProgress returning { totalRaisedEur: 4520, participantCount: 87, donorCount: 112 } produces JSON with raised: 4520, participants: 87, donors: 112
    - --charging-stations 5 includes chargingStations: 5 in output
    - getProgress rejection causes non-zero exit (test via exported helper or spawn with mock)
    - applyFinalStatsPatch replaces only finalStats.raised, .participants, .donors numeric fields in a fixture event.ts string
  </behavior>
  <action>
    Create backend/src/snapshot-final-stats.test.ts. Export buildSnapshot(progress, chargingStations?) and applyFinalStatsPatch(content, snapshot) pure functions from snapshot-final-stats.ts for testability.

    Test buildSnapshot mapping logic without live Sheets credentials.

    Assert output shape matches event.ts finalStats field names (raised, participants, donors, optional chargingStations).

    Test applyFinalStatsPatch with a minimal fixture string containing finalStats block: assert only raised/participants/donors numbers change; thankYouMessage and impactStatement unchanged.

    Do not require live SPREADSHEET_ID in CI.
  </action>
  <verify>
    <automated>npx vitest run backend/src/snapshot-final-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-final-stats.test.ts passes in CI without Sheets credentials
    - Output JSON keys match finalStats shape in event.ts
    - Tests cover chargingStations flag behavior
  </acceptance_criteria>
  <done>Snapshot mapping logic verified without live Sheets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sheets → snapshot script | Reconciled data must be accurate before public freeze |
| Ops → event.ts commit | Human reviews stdout before commit per D-13 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-06 | Spoofing | Stale/incorrect public finalStats | mitigate | Run only after audit-whydonate-records clean (D-02, D-12); ops review stdout before commit (D-13) |
| T-8-07 | Tampering | --apply patches wrong file fields | mitigate | --apply updates only finalStats numeric fields; copy/chargingStations remain manual (D-14, D-16) |
| T-8-SC | Tampering | npm installs | mitigate | No new packages |

</threat_model>

<verification>
- `npx vitest run backend/src/snapshot-final-stats.test.ts`
- `cd backend && npx tsc --noEmit`
- Manual dry-run (ops): `cd backend && npm run snapshot-final-stats` with valid .env prints JSON (not required in CI)
</verification>

<success_criteria>
1. npm run snapshot-final-stats outputs valid JSON from Sheets
2. chargingStations only via CLI flag, never auto-calculated
3. Unit tests pass without live Sheets
4. Script follows existing ops-script conventions
</success_criteria>

<output>
Create `.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-03-SUMMARY.md` when done
</output>
