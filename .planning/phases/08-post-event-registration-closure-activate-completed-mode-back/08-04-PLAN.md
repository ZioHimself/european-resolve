---
phase: 08-post-event-registration-closure-activate-completed-mode-back
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - .github/workflows/deploy-backend.yml
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - POST-02
  - DASH-05
  - EVNT-02
user_setup:
  - service: cloudflare-pages
    why: "Frontend completed mode requires NEXT_PUBLIC_EVENT_STATUS at build time"
    env_vars:
      - name: NEXT_PUBLIC_EVENT_STATUS
        source: "Cloudflare Pages dashboard → Project → Settings → Environment variables → Production"
        value: "completed (set only at closure deploy)"
  - service: github-actions
    why: "Backend closure guards read EVENT_STATUS from Cloud Run env"
    env_vars:
      - name: EVENT_STATUS
        source: "GitHub repo → Settings → Secrets and variables → Actions → Variables"
        value: "completed (set only at closure deploy)"
must_haves:
  truths:
    - "D-01: Manual deploy — env vars flipped only when team triggers closure redeploy"
    - "D-02: Closure deploy only after WhyDonate reconciliation audit is clean"
    - "D-03: Same closure rebuild clears /events Upcoming badge (event date past)"
    - "D-04: Closure steps in plan checkpoint only — no separate RUNBOOK.md"
    - "D-15: finalStats committed before env vars flipped; no closure with placeholder zeros"
    - "D-16: thankYouMessage, impactStatement, chargingStations updated in same closure commit"
    - "CI verifies NEXT_PUBLIC_EVENT_STATUS=completed build succeeds"
    - "deploy-backend.yml passes EVENT_STATUS to Cloud Run with active default"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Completed-mode smoke build step"
      pattern: "NEXT_PUBLIC_EVENT_STATUS=completed"
    - path: .github/workflows/deploy-backend.yml
      provides: "EVENT_STATUS Cloud Run env var"
      pattern: "EVENT_STATUS"
  key_links:
    - from: .github/workflows/deploy-backend.yml
      to: Cloud Run service
      via: "env_vars EVENT_STATUS"
      pattern: "EVENT_STATUS"
    - from: Cloudflare Pages build
      to: src/hooks/useEventStatus.ts
      via: "NEXT_PUBLIC_EVENT_STATUS=completed"
      pattern: "NEXT_PUBLIC_EVENT_STATUS"
    - from: src/data/event.ts
      to: src/components/ui/ProgressSection.tsx
      via: "postEvent.finalStats when completed"
      pattern: "finalStats"
---

<objective>
Wire deploy pipelines for dual env-var closure flip and document the ops closure sequence (reconcile → snapshot → commit → deploy) without a separate RUNBOOK.

Purpose: Activate completed mode per D-01, D-03, D-10, D-15, D-16 in a single coordinated release.
Output: CI completed-mode build gate, deploy-backend.yml EVENT_STATUS, closure ops checklist with human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-RESEARCH.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-PATTERNS.md
@docs/adrs/004-cloudflare-pages-github-app.md

<interfaces>
From .github/workflows/deploy-backend.yml env_vars block:
```yaml
env_vars: |
  SPREADSHEET_ID=${{ vars.SPREADSHEET_ID }}
  NODE_ENV=production
```

Add:
```yaml
EVENT_STATUS=${{ vars.EVENT_STATUS || 'active' }}
```

From .github/workflows/ci.yml:
```yaml
- run: npm run build
```

Add after default build:
```yaml
- run: NEXT_PUBLIC_EVENT_STATUS=completed npm run build
```

From src/components/ui/ProgressSection.tsx (no code change — verify behavior):
- isCompleted skips polling; reads eventDetails.postEvent.finalStats

Gallery skipped per D-17, D-18 — do not activate EventGallery or galleryFolderId in closure checklist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI and deploy workflow wiring</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy-backend.yml</files>
  <read_first>.github/workflows/ci.yml, .github/workflows/deploy-backend.yml, backend/.env.example, docs/adrs/004-cloudflare-pages-github-app.md</read_first>
  <action>
    Per D-10 and research deploy pattern:

    In .github/workflows/deploy-backend.yml env_vars block, add:
      EVENT_STATUS=${{ vars.EVENT_STATUS || 'active' }}
    Place after NODE_ENV=production line. Add comment in workflow that vars.EVENT_STATUS should remain unset or "active" until closure; set to "completed" only at closure deploy.

    In .github/workflows/ci.yml quality job, after existing `npm run build` step, add:
      - run: NEXT_PUBLIC_EVENT_STATUS=completed npm run build
    Name step clearly (e.g. "Build completed mode smoke"). This verifies POST-02 results layout builds without error.

    Optionally add `cd backend && npx tsc --noEmit` to CI if not already covered by root typecheck — only if root npm run typecheck does not include backend.

    Do NOT set NEXT_PUBLIC_EVENT_STATUS or EVENT_STATUS to completed in repo files — closure values are set in Cloudflare dashboard and GitHub vars at ops time per D-01.
  </action>
  <verify>
    <automated>NEXT_PUBLIC_EVENT_STATUS=completed npm run build</automated>
  </verify>
  <acceptance_criteria>
    - deploy-backend.yml contains EVENT_STATUS with || 'active' default
    - ci.yml contains completed-mode smoke build step
    - NEXT_PUBLIC_EVENT_STATUS=completed npm run build succeeds locally
    - No hardcoded "completed" in repo env files committed for production
  </acceptance_criteria>
  <done>CI catches completed-mode build regressions; backend deploy ready for EVENT_STATUS var.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Closure deploy sequence (ops checklist)</name>
  <files>src/data/event.ts</files>
  <read_first>.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md, src/data/event.ts, backend/src/snapshot-final-stats.ts</read_first>
  <action>
    Execute the one-time closure release per D-01, D-02, D-15, D-16 after Plans 01–03 and Task 1 are merged. Follow the numbered steps in how-to-verify exactly. Human commits only: update src/data/event.ts with snapshot values and impact copy, set GitHub vars.EVENT_STATUS=completed and Cloudflare NEXT_PUBLIC_EVENT_STATUS=completed, then push to main. Do not flip env vars before finalStats commit.
  </action>
  <what-built>Automated closure infrastructure from Plans 01–03 and deploy wiring from Task 1. This checkpoint executes the one-time closure release per D-01, D-02, D-15, D-16.</what-built>
  <how-to-verify>
    Execute in order (do NOT skip steps; do NOT flip env vars before finalStats commit per D-15):

    **Pre-closure gate (D-02)**
    1. Run `cd backend && npm run audit-whydonate-records` until output is clean (all WD payments traced).
    2. Run `cd backend && npm run sync-whydonate-tracking` if any pending sync remains.

    **Snapshot (D-12, D-13, D-14)**
    3. Run `cd backend && npm run snapshot-final-stats` — review stdout JSON (raised, participants, donors).
    4. Obtain real chargingStations count from Hurkit confirmation (manual, D-14).

    **Commit event.ts (D-15, D-16)**
    5. Update src/data/event.ts in one commit:
       - postEvent.finalStats.raised, .participants, .donors from snapshot JSON
       - postEvent.finalStats.chargingStations from Hurkit number
       - postEvent.thankYouMessage and postEvent.impactStatement with final copy (no em dash in user-facing text)
       - Do NOT set galleryFolderId (D-17, D-18)
    6. Verify values are non-zero / real — never deploy closure with placeholder zeros (D-15).

    **Flip env vars (D-01, D-10) — same deploy window**
    7. GitHub: Settings → Secrets and variables → Actions → Variables → set EVENT_STATUS=completed
    8. Cloudflare Pages: Project → Settings → Environment variables → Production → set NEXT_PUBLIC_EVENT_STATUS=completed

    **Deploy (D-03)**
    9. Push closure commit to main (triggers CI + Cloudflare rebuild + backend deploy).
    10. Confirm event date 2026-08-23 is past so /events rebuild clears Upcoming badge (D-03).

  **Post-deploy verification**
    11. Visit https://european-resolve.org/events/2026-run-for-ukraine/ — results layout (not TrackCards), frozen stats match committed finalStats, no live polling. Confirm AccountabilityReport shows postEvent.impactStatement and finalStats.chargingStations from committed event.ts (DASH-04).
    12. Visit /events/2026-run-for-ukraine/register — closed banner (no token).
    13. Visit /events/2026-run-for-ukraine/register?token=&lt;valid-pending-token&gt; — payment flow visible (D-06).
    14. curl -X POST https://&lt;backend-url&gt;/api/register -H "Content-Type: application/json" -d '{}' → 403 REGISTRATION_CLOSED.
    15. curl -X POST https://&lt;backend-url&gt;/api/donors -H "Content-Type: application/json" -d '{}' → 403 REGISTRATION_CLOSED.
    16. Existing fundraiser page GET still loads; POST edit returns 403.
  </how-to-verify>
  <verify>
    <human-check>Complete all 16 post-deploy verification steps in how-to-verify; confirm production matches acceptance_criteria</human-check>
  </verify>
  <acceptance_criteria>
    - finalStats in production match reconciled snapshot values
    - Frontend and backend both in completed mode
    - Write endpoints return 403; confirm-payment still works for pending token
    - /events page shows no Upcoming badge for Run for Ukraine 2026
    - Gallery section absent or empty (acceptable per D-17)
  </acceptance_criteria>
  <done>Closure deploy executed; production verified in completed mode with frozen stats.</done>
  <resume-signal>Type "closure-deployed" with verification notes, or describe any failed step</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD → production env vars | Incorrect var timing could close site with wrong stats |
| Public → results page | Frozen stats must match reconciled truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-08 | Tampering | Frontend/backend status mismatch | mitigate | Flip both env vars in same deploy window; verify curl tests post-deploy (D-15) |
| T-8-09 | Spoofing | Misleading €0 frozen stats | mitigate | Block deploy until finalStats committed with real numbers (D-15 checkpoint step 6) |
| T-8-10 | Denial of Service | Premature closure | mitigate | Manual ops trigger only after reconciliation clean (D-01, D-02) |
| T-8-SC | Tampering | Workflow file changes | mitigate | Code review on deploy-backend.yml and ci.yml changes |

</threat_model>

<verification>
- `NEXT_PUBLIC_EVENT_STATUS=completed npm run build` (CI step)
- `npm test && cd backend && npx tsc --noEmit`
- Post-deploy manual checks in Task 2 checkpoint
</verification>

<success_criteria>
1. CI includes completed-mode build smoke test
2. Backend deploy passes EVENT_STATUS with safe default
3. Closure ops sequence documented and executed with human verification
4. Production site shows results page with frozen reconciled stats
5. Write API blocked; late payment flow works
</success_criteria>

<output>
Create `.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-04-SUMMARY.md` when done
</output>
