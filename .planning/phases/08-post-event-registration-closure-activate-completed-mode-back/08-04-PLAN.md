---
phase: 08-post-event-registration-closure-activate-completed-mode-back
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - .github/workflows/deploy-backend.yml
  - .github/workflows/ci.yml
  - src/components/ui/AccountabilityReport.tsx
  - src/__tests__/AccountabilityReport.test.tsx
  - backend/src/snapshot-final-stats.ts
  - src/data/event.ts
autonomous: false
requirements:
  - POST-02
  - DASH-04
  - DASH-05
  - EVNT-02
user_setup:
  - service: cloudflare-pages
    why: "Frontend completed mode requires NEXT_PUBLIC_EVENT_STATUS at build time"
    env_vars:
      - name: NEXT_PUBLIC_EVENT_STATUS
        source: "Cloudflare Pages dashboard → Project → Settings → Environment variables → Production"
        value: "completed (set only at closure deploy)"
  - service: github-actions
    why: "Backend closure guards read EVENT_STATUS from Cloud Run env"
    env_vars:
      - name: EVENT_STATUS
        source: "GitHub repo → Settings → Secrets and variables → Actions → Variables"
        value: "completed (set only at closure deploy)"
must_haves:
  truths:
    - "D-01: Manual deploy — env vars flipped only when team triggers closure redeploy"
    - "D-02: Closure deploy only after WhyDonate reconciliation audit is clean"
    - "D-03: Same closure rebuild clears /events Upcoming badge (event date past)"
    - "D-04: Closure steps in plan checkpoint only — no separate RUNBOOK.md"
    - "D-14: chargingStations is not a closure blocker; may remain 0 until Hurkit follow-up deploy"
    - "D-15: Reconciled raised/participants/donors committed before env vars flipped; no closure with placeholder/zero donation stats"
    - "D-16: Closure commit updates thankYouMessage and impactStatement alongside reconciled finalStats (not chargingStations)"
    - "D-19: AccountabilityReport hides charging-stations stat when finalStats.chargingStations is 0"
    - "CI verifies NEXT_PUBLIC_EVENT_STATUS=completed build succeeds"
    - "deploy-backend.yml passes EVENT_STATUS to Cloud Run with active default"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Completed-mode smoke build step"
      pattern: "NEXT_PUBLIC_EVENT_STATUS=completed"
    - path: .github/workflows/deploy-backend.yml
      provides: "EVENT_STATUS Cloud Run env var"
      pattern: "EVENT_STATUS"
    - path: src/components/ui/AccountabilityReport.tsx
      provides: "Conditional charging-stations stat (D-19)"
      pattern: "chargingStations > 0"
    - path: src/__tests__/AccountabilityReport.test.tsx
      provides: "D-19 regression test"
      min_lines: 30
  key_links:
    - from: .github/workflows/deploy-backend.yml
      to: Cloud Run service
      via: "env_vars EVENT_STATUS"
      pattern: "EVENT_STATUS"
    - from: Cloudflare Pages build
      to: src/hooks/useEventStatus.ts
      via: "NEXT_PUBLIC_EVENT_STATUS=completed"
      pattern: "NEXT_PUBLIC_EVENT_STATUS"
    - from: src/data/event.ts
      to: src/components/ui/ProgressSection.tsx
      via: "postEvent.finalStats when completed"
      pattern: "finalStats"
    - from: src/data/event.ts
      to: src/components/ui/AccountabilityReport.tsx
      via: "finalStats.chargingStations conditional render"
      pattern: "chargingStations"
---

<objective>
Finish Phase 8 closure: verify deploy wiring (Plans 01–03 already shipped), implement D-19 so results page never shows "0 charging stations funded", then execute the one-time closure deploy checkpoint.

Purpose: Activate completed mode per D-01, D-03, D-10, D-15, D-16 in a single coordinated release without waiting for Hurkit station count.
Output: Verified CI/deploy env wiring, AccountabilityReport D-19 fix + test, closure ops checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-01-SUMMARY.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-02-SUMMARY.md
@.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-03-SUMMARY.md
@docs/adrs/004-cloudflare-pages-github-app.md

**Already implemented (do not redo):**
- Backend write guards + `EVENT_STATUS` config (08-01)
- Register page `?token=` exception (08-02)
- `snapshot-final-stats` ops script (08-03)

**May already be present in repo** — Task 1 verifies rather than blindly re-adding:
- `.github/workflows/ci.yml` step `NEXT_PUBLIC_EVENT_STATUS=completed npm run build`
- `.github/workflows/deploy-backend.yml` line `EVENT_STATUS=${{ vars.EVENT_STATUS || 'active' }}`

<interfaces>
From src/components/ui/AccountabilityReport.tsx (current — always renders charging stations):
```tsx
<div className={styles.statCard}>
  <dt className={styles.statLabel}>{t("closed.chargingStations")}</dt>
  <dd className={styles.statValue}>{finalStats.chargingStations}</dd>
</div>
```

Target D-19 pattern:
```tsx
{finalStats.chargingStations > 0 && (
  <div className={styles.statCard}>...</div>
)}
```

From backend/src/snapshot-final-stats.ts (--apply warning — update to match D-14/D-16):
```typescript
console.warn(
  "Update thankYouMessage and impactStatement manually before closure commit.",
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI and deploy workflow wiring</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy-backend.yml</files>
  <read_first>.github/workflows/ci.yml, .github/workflows/deploy-backend.yml, backend/.env.example, docs/adrs/004-cloudflare-pages-github-app.md, .planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-01-SUMMARY.md</read_first>
  <action>
    Per D-10: confirm deploy wiring exists. **If already present, do not duplicate** — only add missing pieces.

    Required in `.github/workflows/deploy-backend.yml` env_vars block:
      EVENT_STATUS=${{ vars.EVENT_STATUS || 'active' }}
    With comment that vars.EVENT_STATUS stays unset or "active" until closure; set to "completed" only at closure deploy.

    Required in `.github/workflows/ci.yml` quality job, after `npm run build`:
      - name: Build completed mode smoke
        run: NEXT_PUBLIC_EVENT_STATUS=completed npm run build

    Do NOT set NEXT_PUBLIC_EVENT_STATUS or EVENT_STATUS to "completed" in committed repo files (D-01). Values are set in Cloudflare dashboard and GitHub Actions variables at ops time.

    If both requirements are already satisfied, make no workflow edits and note "verified" in SUMMARY.
  </action>
  <verify>
    <automated>NEXT_PUBLIC_EVENT_STATUS=completed npm run build</automated>
  </verify>
  <acceptance_criteria>
    - deploy-backend.yml contains EVENT_STATUS with || 'active' default
    - ci.yml contains completed-mode smoke build step named clearly
    - NEXT_PUBLIC_EVENT_STATUS=completed npm run build succeeds
    - No hardcoded production "completed" in repo env files
  </acceptance_criteria>
  <done>CI catches completed-mode build regressions; backend deploy ready for EVENT_STATUS var.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hide charging-stations stat when unknown (D-19)</name>
  <files>src/components/ui/AccountabilityReport.tsx, src/__tests__/AccountabilityReport.test.tsx, backend/src/snapshot-final-stats.ts</files>
  <read_first>src/components/ui/AccountabilityReport.tsx, src/components/ui/AccountabilityReport.module.css, src/data/event.ts, src/locales/en.ts, src/__tests__/register-page-completed.test.tsx, backend/src/snapshot-final-stats.ts</read_first>
  <behavior>
    - When finalStats.chargingStations is 0: charging-stations stat card not in DOM; totalRaised stat still visible; impactStatement still visible
    - When finalStats.chargingStations is greater than 0: both stat cards visible with correct values
  </behavior>
  <action>
    RED: Create src/__tests__/AccountabilityReport.test.tsx. vi.mock @/data/event with postEvent.finalStats.chargingStations: 0 and assert t("closed.chargingStations") text is absent while t("closed.totalRaised") is present. Second case: mock chargingStations: 5 and assert charging label present.

    GREEN: In AccountabilityReport.tsx, wrap the charging-stations statCard in `{finalStats.chargingStations > 0 && (...)}`. Keep totalRaised and impactStatement unchanged.

    Minor consistency fix (08-03 script message, not a replan): in backend/src/snapshot-final-stats.ts --apply branch, change console.warn to mention only thankYouMessage and impactStatement (remove chargingStations from closure-commit wording per D-14/D-16).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/AccountabilityReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AccountabilityReport.tsx conditionally renders charging stat only when finalStats.chargingStations > 0
    - Test passes for chargingStations: 0 (hidden) and chargingStations: 5 (visible)
    - snapshot-final-stats.ts --apply warning no longer says chargingStations is required at closure
    - npm test passes with no regressions
  </acceptance_criteria>
  <done>D-19 satisfied: closure deploy safe with chargingStations: 0 in event.ts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Closure deploy sequence (ops checklist)</name>
  <files>src/data/event.ts</files>
  <read_first>.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-CONTEXT.md, src/data/event.ts, backend/src/snapshot-final-stats.ts, .planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-03-SUMMARY.md</read_first>
  <action>
    Execute the one-time closure release per D-01, D-02, D-15, D-16 after Plans 01–03 and Tasks 1–2 are merged. Human commits only: update src/data/event.ts with reconciled snapshot values and impact copy, set GitHub vars.EVENT_STATUS=completed and Cloudflare NEXT_PUBLIC_EVENT_STATUS=completed, then push to main. Do not flip env vars before finalStats commit. chargingStations may stay 0 (D-14); Hurkit update is a later follow-up deploy.
  </action>
  <what-built>Backend guards, register token exception, snapshot script, deploy wiring, and D-19 UI from Plans 01–04. This checkpoint flips production to completed mode.</what-built>
  <how-to-verify>
    Execute in order (do NOT flip env vars before finalStats commit per D-15):

    **Pre-closure gate (D-02)**
    1. Run `cd backend && npm run audit-whydonate-records` until output is clean (all WD payments traced).
    2. Run `cd backend && npm run sync-whydonate-tracking` if any pending sync remains.

    **Snapshot (D-12, D-13)**
    3. Run `cd backend && npm run snapshot-final-stats` — review stdout JSON (`raised`, `participants`, `donors`).
    4. Optional: `npm run snapshot-final-stats -- --apply` to patch numeric fields, then manually edit copy in event.ts.

    **Commit event.ts (D-15, D-16)**
    5. Update src/data/event.ts in one closure commit:
       - postEvent.finalStats.raised, .participants, .donors from reconciled snapshot (must be real, non-zero donation totals)
       - postEvent.thankYouMessage and postEvent.impactStatement with final copy (no em dash in user-facing text)
       - postEvent.finalStats.chargingStations may remain `0` until Hurkit confirms (D-14) — D-19 hides the stat
       - Do NOT set galleryFolderId (D-17, D-18)
    6. Verify donation stats (raised, participants, donors) are reconciled real values — never deploy closure with placeholder zeros (D-15).

    **Flip env vars (D-01, D-10) — same deploy window**
    7. GitHub: Settings → Secrets and variables → Actions → Variables → set EVENT_STATUS=completed
    8. Cloudflare Pages: Project → Settings → Environment variables → Production → set NEXT_PUBLIC_EVENT_STATUS=completed

    **Deploy (D-03)**
    9. Push closure commit to main (triggers CI + Cloudflare rebuild + backend deploy).
    10. Confirm event date 2026-08-23 is past so /events rebuild clears Upcoming badge (D-03).

    **Post-deploy verification**
    11. Visit https://european-resolve.org/events/2026-run-for-ukraine/ — results layout (not TrackCards), frozen donation stats match committed finalStats, no live polling. AccountabilityReport shows impactStatement; no "0 charging stations funded" when chargingStations is 0 (D-19, DASH-04).
    12. Visit /events/2026-run-for-ukraine/register — closed banner (no token).
    13. Visit /events/2026-run-for-ukraine/register?token=&lt;valid-pending-token&gt; — payment flow visible (D-06).
    14. curl -X POST https://&lt;backend-url&gt;/api/register -H "Content-Type: application/json" -d '{}' → 403 REGISTRATION_CLOSED.
    15. curl -X POST https://&lt;backend-url&gt;/api/donors -H "Content-Type: application/json" -d '{}' → 403 REGISTRATION_CLOSED.
    16. Existing fundraiser page GET still loads; POST edit returns 403.
  </how-to-verify>
  <verify>
    <human-check>Complete all 16 post-deploy verification steps in how-to-verify; confirm production matches acceptance_criteria</human-check>
  </verify>
  <acceptance_criteria>
    - Reconciled donation finalStats in production (not €0 placeholders)
    - Frontend and backend both in completed mode
    - Write endpoints return 403; confirm-payment still works for pending token
    - /events page shows no Upcoming badge for Run for Ukraine 2026
    - Results page does not display charging-stations stat when chargingStations is 0 (D-19)
    - Gallery section absent or empty (acceptable per D-17)
  </acceptance_criteria>
  <done>Closure deploy executed; production verified in completed mode with frozen reconciled donation stats.</done>
  <resume-signal>Type "closure-deployed" with verification notes, or describe any failed step</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD → production env vars | Incorrect var timing could close site with wrong stats |
| Public → results page | Frozen donation stats must match reconciled truth; misleading zero station count must not display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-08 | Tampering | Frontend/backend status mismatch | mitigate | Flip both env vars in same deploy window; verify curl tests post-deploy (D-15) |
| T-8-09 | Spoofing | Misleading €0 frozen donation stats | mitigate | Block deploy until raised/participants/donors committed with reconciled values (D-15 checkpoint step 6) |
| T-8-11 | Spoofing | "0 charging stations funded" shown as final result | mitigate | D-19 conditional render in AccountabilityReport |
| T-8-10 | Denial of Service | Premature closure | mitigate | Manual ops trigger only after reconciliation clean (D-01, D-02) |
| T-8-SC | Tampering | Workflow file changes | mitigate | Code review on deploy-backend.yml and ci.yml changes |

</threat_model>

<verification>
- `npx vitest run src/__tests__/AccountabilityReport.test.tsx`
- `NEXT_PUBLIC_EVENT_STATUS=completed npm run build`
- `npm test && cd backend && npx tsc --noEmit`
- Post-deploy manual checks in Task 3 checkpoint
</verification>

<success_criteria>
1. CI includes completed-mode build smoke test (verified or added)
2. Backend deploy passes EVENT_STATUS with safe default
3. D-19: no misleading zero charging-stations stat at closure
4. Closure ops sequence executed with human verification
5. Production shows reconciled donation totals; write APIs blocked; late payment flow works
</success_criteria>

<output>
Create `.planning/phases/08-post-event-registration-closure-activate-completed-mode-back/08-04-SUMMARY.md` when done
</output>
