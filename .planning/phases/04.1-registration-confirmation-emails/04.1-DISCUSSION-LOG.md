# Phase 04.1: Registration Confirmation Emails - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-30
**Phase:** 04.1-registration-confirmation-emails
**Areas discussed:** SMTP deployment & secrets, Fundraiser+registration email gap

---

## SMTP Secrets Deployment

### How to pass SMTP credentials to Cloud Run

| Option | Description | Selected |
|--------|-------------|----------|
| GCP Secret Manager | Store SMTP_PASS as a secret, reference it in Cloud Run. More secure, native to GCP. | |
| GitHub Secrets as env_vars | Pass SMTP_PASS via `${{ secrets.SMTP_PASS }}` in the deploy workflow. Simpler, keeps everything in GitHub. | ✓ |
| You decide | Pick the best approach for this project's scale | |

**User's choice:** GitHub Secrets as env_vars
**Notes:** Simpler approach appropriate for the project's scale.

### SMTP Provider Confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| mailbox.org confirmed | We have a mailbox.org account for european-resolve.org | ✓ |
| Different provider | Let me specify | |

**User's choice:** mailbox.org confirmed
**Notes:** None.

### DNS Records (SPF/DKIM/DMARC)

| Option | Description | Selected |
|--------|-------------|----------|
| Already configured | DNS records are set up (we already send/receive email from this domain) | ✓ |
| Need a checklist | Include DNS record setup steps in the plan | |
| Not sure | Need to verify | |

**User's choice:** Already configured
**Notes:** Domain already sends/receives email, DNS deliverability records in place.

---

## Fundraiser+Registration Email Gap

### Email content for combined fundraiser+registration

| Option | Description | Selected |
|--------|-------------|----------|
| Same registration + fundraiser link | Reuse existing email template, add a section with fundraiser URL and edit token | |
| Same registration only | Reuse existing email as-is, no fundraiser info | |
| Two separate emails | One for registration, one for fundraiser page creation | |

**User's choice:** Two separate email templates — one for registration only, one for fundraiser (also containing the fundraiser edit link)
**Notes:** User clarified: two distinct templates. The fundraiser email should contain both registration details AND fundraiser details (page URL, edit link).

### Fundraiser email content scope

| Option | Description | Selected |
|--------|-------------|----------|
| Registration + fundraiser details | Participant ID, tier, rewards, PLUS fundraiser page URL, edit link, display name, goal | ✓ |
| Fundraiser details only | Fundraiser page URL, edit link, display name, goal. No registration info. | |
| You decide | Design the best email for this combined flow | |

**User's choice:** Full both — registration details plus fundraiser details
**Notes:** None.

### Standalone fundraiser email

**User's observation:** "Wait, fundraiser always registers, doesn't it?"
**Clarification:** The frontend always uses `POST /api/fundraiser/register` (combined flow). The standalone `POST /api/fundraiser` endpoint exists in the backend but is not called by the frontend. No email needed for the unused standalone endpoint.

---

## Claude's Discretion

- Fundraiser email HTML template layout and visual design
- How to structure the edit link URL
- Whether to extract shared email sections into reusable partials
- Exact fundraiser-specific locale string keys and copy

## Deferred Ideas

- Pre-event reminder emails (ADMN-04)
- Race-day logistics email (ADMN-05)
- Post-event email (POST-01)
- Email retry queue
- Email open/click tracking
