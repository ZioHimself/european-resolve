---
phase: 06.1-tier-amount-enforcement-effective-tier-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/types.ts
  - backend/src/tiers.ts
  - backend/src/services/sheets.ts
  - backend/src/email/locales/types.ts
  - backend/src/email/locales/en.ts
  - backend/src/email/locales/fr.ts
  - backend/src/email/locales/uk.ts
  - backend/src/email/locales/nl.ts
  - backend/src/email/locales/de.ts
  - backend/src/tiers.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - REGA-06
must_haves:
  truths:
    - "confirm-payment returns tierName and rewards derived from observed amount, not registered tier"
    - "Payments below €10 resolve to donor tier with thank-you-only rewards (D-14)"
    - "Gate bypass (devtools underpay) still confirms with downgraded effective tier (D-13)"
    - "Payment confirmation email uses same effective tier and cumulative rewards as API response (D-11)"
    - "Sheets column H (registered tier intent) is never overwritten at confirm"
  artifacts:
    - path: backend/src/tiers.ts
      provides: getEffectiveTier, getCumulativeRewards, donor tier in TIER_DATA
      exports: ["getEffectiveTier", "getCumulativeRewards", "TIER_DATA"]
    - path: backend/src/tiers.test.ts
      provides: boundary tests for effective tier and cumulative merge
    - path: backend/src/services/sheets.ts
      provides: confirmPayment effective-tier return block
      contains: "getEffectiveTier"
  key_links:
    - from: backend/src/services/sheets.ts
      to: backend/src/tiers.ts
      via: getEffectiveTier(recordedAmount) + getCumulativeRewards
      pattern: "getEffectiveTier\\(recordedAmount\\)"
    - from: backend/src/routes/confirm-payment.ts
      to: backend/src/services/sheets.ts
      via: result.tierName / result.rewards pass-through to email
      pattern: "sendPaymentConfirmationEmail"
---

<objective>
Implement backend-authoritative effective tier computation, cumulative reward merge, donor floor tier, and wire confirmPayment + payment email to use observed amount only.

Purpose: Client step-1 gate is UX-only (D-13); backend must assign rewards from money received per ADR-009.
Output: `getEffectiveTier()`, `getCumulativeRewards()`, updated `confirmPayment()` return values, `donor` tier in all email locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-CONTEXT.md
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-RESEARCH.md
@docs/adrs/009-tier-amount-enforcement-effective-tier.md

<interfaces>
From backend/src/types.ts:
```typescript
export type TierId = "supporter" | "sprinter" | "relay-runner" | "marathoner" | "ultramarathoner";
export interface ConfirmPaymentResponse {
  confirmed: boolean;
  participantId: string;
  tierName: string;
  amountEur?: number;
  rewards: string[];
}
```

From backend/src/tiers.ts (current):
```typescript
export const TIER_DATA: Record<TierId, { name: string; price: number }>;
export function getLocalizedRewards(tierId: TierId, language: string): string[];
export function getTierPrice(tierId: string): number;
```

From backend/src/services/sheets.ts confirmPayment return gap (lines 240-262):
- Currently returns registered tier name + getLocalizedRewards(registeredTierId)
- Must change to getEffectiveTier(recordedAmount) when amount known
- row[7] (tier intent column) must NOT be updated in Sheets write (lines 223-238)

Thresholds per ADR-009: €10 / €15 / €30 / €60 / €100; donor for < €10 (D-14).
VALID_TIER_IDS in register.ts and fundraiser.ts must NOT include "donor".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — backend tier test scaffold + vitest include</name>
  <files>vitest.config.ts, backend/src/tiers.test.ts</files>
  <read_first>
    - vitest.config.ts (current test config — no backend include)
    - backend/src/tiers.ts (exports to test against)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-VALIDATION.md (Wave 0 gaps)
    - src/__tests__/events.test.ts (pure function test pattern)
  </read_first>
  <behavior>
    - getEffectiveTier(9.99) → "donor"
    - getEffectiveTier(10) → "supporter"
    - getEffectiveTier(15) → "sprinter"
    - getEffectiveTier(30) → "relay-runner"
    - getEffectiveTier(60) → "marathoner"
    - getEffectiveTier(100) → "ultramarathoner"
    - getEffectiveTier(150) → "ultramarathoner"
    - getEffectiveTier(0) and getEffectiveTier(NaN) → "donor"
    - getCumulativeRewards("donor", "English") returns single thank-you string array
    - getCumulativeRewards("relay-runner", "English") merges supporter through relay-runner with dedupe
  </behavior>
  <action>
    Add `include: ["src/**/*.test.{ts,tsx}", "backend/src/**/*.test.ts"]` to vitest.config.ts test block so backend unit tests run under `npm test`.

    Create backend/src/tiers.test.ts importing getEffectiveTier and getCumulativeRewards from "./tiers.js". Write failing tests for all boundary values listed in behavior block. Tests will fail until Task 2 implements the functions — that is expected RED state.
  </action>
  <acceptance_criteria>
    - vitest.config.ts includes backend/src/**/*.test.ts pattern
    - backend/src/tiers.test.ts exists with describe blocks for getEffectiveTier and getCumulativeRewards
    - Running `npx vitest run backend/src/tiers.test.ts -x` executes (may fail RED until Task 2)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run backend/src/tiers.test.ts -x 2>&1 | head -5</automated>
  </verify>
  <done>Backend test harness exists and tier boundary tests are written (RED acceptable pre-implementation)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getEffectiveTier, getCumulativeRewards, and donor tier (D-12, D-14)</name>
  <files>backend/src/types.ts, backend/src/tiers.ts, backend/src/email/locales/types.ts</files>
  <read_first>
    - backend/src/types.ts (TierId union line 1)
    - backend/src/tiers.ts (TIER_DATA, getLocalizedRewards)
    - backend/src/email/locales/types.ts (tierRewards Record type)
    - docs/adrs/009-tier-amount-enforcement-effective-tier.md (threshold table)
    - backend/src/routes/register.ts (VALID_TIER_IDS — do NOT add donor)
  </read_first>
  <action>
    Extend TierId in backend/src/types.ts: add `"donor"` to union.

    In backend/src/tiers.ts:
    1. Add `donor: { name: "Donor", price: 0 }` to TIER_DATA.
    2. Add TIER_THRESHOLDS constant sorted descending by min: ultramarathoner 100, marathoner 60, relay-runner 30, sprinter 15, supporter 10, donor 0.
    3. Export getEffectiveTier(amountEur: number): TierId — walk thresholds; non-finite or <= 0 returns "donor"; first match where amountEur >= min wins.
    4. Export getCumulativeRewards(tierId: TierId, language: string): string[] — for "donor" return getLocalizedRewards("donor", language) only; for runner tiers iterate REWARD_TIER_ORDER ["supporter","sprinter","relay-runner","marathoner","ultramarathoner"], merge split rewards with Set dedupe, stop at effective tier.

    Do NOT add donor to VALID_TIER_IDS in register.ts or fundraiser.ts.

    Email locale tierRewards.donor strings deferred to Task 3 — stub getLocalizedRewards to handle missing donor key gracefully or add placeholder in Task 3 before full test pass.
  </action>
  <acceptance_criteria>
    - TierId includes "donor"
    - TIER_DATA.donor exists with price 0
    - getEffectiveTier boundary tests from Task 1 all pass
    - getCumulativeRewards("donor") returns array length >= 1 once locales updated
    - register.ts VALID_TIER_IDS unchanged (5 selectable tiers only)
    - cd backend && npx tsc --noEmit passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run backend/src/tiers.test.ts -x</automated>
  </verify>
  <done>getEffectiveTier and getCumulativeRewards implemented; tier boundary tests green</done>
</task>

<task type="auto">
  <name>Task 3: Wire confirmPayment + email locales to effective tier (D-10, D-11, D-13)</name>
  <files>backend/src/services/sheets.ts, backend/src/email/locales/en.ts, backend/src/email/locales/fr.ts, backend/src/email/locales/uk.ts, backend/src/email/locales/nl.ts, backend/src/email/locales/de.ts</files>
  <read_first>
    - backend/src/services/sheets.ts (confirmPayment lines 215-263)
    - backend/src/routes/confirm-payment.ts (response + sendPaymentConfirmationEmail pass-through)
    - backend/src/email/locales/en.ts (tierRewards block lines 50-58)
    - backend/src/email/locales/uk.ts (Ukrainian copy conventions — informal ti, no em dashes)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-CONTEXT.md (D-11, D-13)
  </read_first>
  <action>
    In sheets.ts confirmPayment, replace lines 240-262 (registered-tier comment and logic):
    - When recordedAmount is defined: effectiveTierId = getEffectiveTier(recordedAmount); tierName = TIER_DATA[effectiveTierId].name; rewards = getCumulativeRewards(effectiveTierId, language).
    - When recordedAmount is undefined: tierName from registered tier name only; rewards = [] (existing unknown-amount behavior).
    - Remove obsolete comment "paying more never upgrades it".
    - Import getEffectiveTier, getCumulativeRewards from ../tiers.js.
    - Do NOT write effective tier to row[7] — Sheets tier column stays registration intent.

    Add tierRewards.donor to all five locale files (en, fr, uk, nl, de):
    - en: short thank-you for sub-€10 donors, no runner rewards (e.g. "Thank you for supporting Ukraine's defenders")
    - uk: informal ti register, gender-neutral, no em dashes per CONVENTIONS.md
    - fr/nl/de: equivalent concise thank-you

    Verify confirm-payment.ts already passes result.tierName and result.rewards to sendPaymentConfirmationEmail — no route logic change needed unless type errors from TierId extension.

    Add unit test in tiers.test.ts: getCumulativeRewards for marathoner includes items from supporter, sprinter, relay-runner tiers (deduped).
  </action>
  <acceptance_criteria>
    - sheets.ts confirmPayment calls getEffectiveTier when recordedAmount is set
    - €8 payment with €30 registered tier would return donor tierName and donor rewards (D-13 silent downgrade)
    - €100 payment returns ultramarathoner tierName with merged cumulative rewards
    - tierRewards.donor present in all 5 locale files
    - grep confirms row[7] is read but not written in confirmPayment update range M:P
    - npx vitest run backend/src/tiers.test.ts -x passes
    - cd backend && npx tsc --noEmit passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run backend/src/tiers.test.ts -x && cd backend && npx tsc --noEmit</automated>
  </verify>
  <done>confirmPayment and payment email use effective tier + cumulative rewards; donor locale strings added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → confirm-payment API | Untrusted amount in POST body; used for tier assignment only |
| confirm-payment → Google Sheets | Authoritative persistence; registered tier intent preserved |
| confirm-payment → SMTP email | Payment receipt reflects backend-computed tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Tampering | confirm-payment amount body | mitigate | getEffectiveTier(observedAmount) on every confirm; never use registered tier for rewards when amount known (D-13) |
| T-06.1-02 | Tampering | Inflated amount claim | accept | Tier assignment only; ops reconcile against WhyDonate dashboard (existing policy) |
| T-06.1-03 | Tampering | Sheets tier column overwrite | mitigate | confirmPayment update range M:P only — do not write col H effective tier |
| T-06.1-04 | Information Disclosure | donor tier email content | accept | Thank-you only; no PII beyond existing payment email |
| T-06.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run backend/src/tiers.test.ts -x` green
- `cd backend && npx tsc --noEmit` green
- Manual spot-check: confirmPayment return for amount=8 with registered relay-runner returns Donor tier, not Relay runner
</verification>

<success_criteria>
- Backend computes effective tier from five thresholds + donor floor
- Cumulative rewards merge with dedupe through effective tier
- confirm-payment API and payment email both expose effective tier (not registered tier) when amount known
- Registered tier intent in Sheets unchanged
</success_criteria>

<output>
Create `.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-01-SUMMARY.md` when done
</output>
