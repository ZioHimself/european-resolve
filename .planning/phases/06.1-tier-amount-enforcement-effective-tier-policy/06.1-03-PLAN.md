---
phase: 06.1-tier-amount-enforcement-effective-tier-policy
plan: 03
type: execute
wave: 3
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - src/components/ui/ConfirmationPanel.tsx
  - src/components/ui/FundraiserConfirmation.tsx
  - src/__tests__/ConfirmationPanel.test.tsx
autonomous: true
requirements:
  - REGA-06
must_haves:
  truths:
    - "Post-payment confirmation panel shows effective tier name from confirm-payment API, not registration selection (D-10)"
    - "Post-payment rewards list shows cumulative effective rewards from API response (D-12)"
    - "No selected-vs-effective comparison or upgrade callout when effective > selected (D-10)"
    - "Track B FundraiserConfirmation shows effective tier parity with Track A after payment"
    - "Displayed amount uses data.data.amountEur from confirm-payment when available"
  artifacts:
    - path: src/components/ui/ConfirmationPanel.tsx
      provides: effectivePayment state from API; confirmed view renders API tier/rewards
      contains: "setEffectivePayment"
    - path: src/components/ui/FundraiserConfirmation.tsx
      provides: effective tier display in confirmed banner/summary
    - path: src/__tests__/ConfirmationPanel.test.tsx
      provides: component test for effective tier override
  key_links:
    - from: src/components/ui/ConfirmationPanel.tsx
      to: /api/register/confirm-payment
      via: handleAutoConfirm stores data.data.tierName, rewards, amountEur
      pattern: "setEffectivePayment"
    - from: src/components/ui/ConfirmationPanel.tsx confirmed branch
      to: effectivePayment state
      via: render tierName and rewards from effectivePayment not result
      pattern: "effectivePayment\\.tierName"
---

<objective>
Store confirm-payment API response in confirmation panel state and render effective tier, cumulative rewards, and paid amount in post-payment UI for Track A and Track B.

Purpose: On-page confirmation must match payment email (D-10, D-11); registration result.tierName is pre-payment intent only.
Output: effectivePayment state in both panels; ConfirmationPanel component test; no upgrade/downgrade callouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-CONTEXT.md
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-PATTERNS.md
@src/components/ui/ConfirmationPanel.tsx
@src/components/ui/FundraiserConfirmation.tsx

<interfaces>
Confirm-payment API response shape (backend/src/types.ts):
```typescript
interface ConfirmPaymentResponse {
  confirmed: boolean;
  participantId: string;
  tierName: string;      // effective tier name after Plan 01
  amountEur?: number;
  rewards: string[];     // cumulative effective rewards
}
```

Current gap — ConfirmationPanel confirmed branch (lines 142-170):
- Renders result.tierName and result.rewards from registration
- Must switch to API response after successful confirm

Proposed state:
```typescript
const [effectivePayment, setEffectivePayment] = useState<{
  tierName: string;
  rewards: string[];
  amountEur?: number;
} | null>(null);
```

Pre-payment panel may continue showing result.tierName / result.amountEur as payment ask (deferred copy tweak per CONTEXT).
Post-payment (confirmed === true) must use effectivePayment only.

FundraiserConfirmation confirmed branch is minimal banner — add tier/rewards summary matching ConfirmationPanel structure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfirmationPanel stores and renders effective tier from API (D-10, D-12)</name>
  <files>src/components/ui/ConfirmationPanel.tsx</files>
  <read_first>
    - src/components/ui/ConfirmationPanel.tsx (handleAutoConfirm, handleManualConfirm, confirmed branch lines 142-170)
    - backend/src/types.ts (ConfirmPaymentResponse)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-CONTEXT.md (D-10 — no upgrade callout)
  </read_first>
  <behavior>
    - On confirm success with data.data.tierName and data.data.rewards: setEffectivePayment from API fields including amountEur
    - Confirmed view tier row shows effectivePayment.tierName, not result.tierName
    - Confirmed view rewards list maps effectivePayment.rewards
    - When effectivePayment.amountEur defined, show paid amount in summary (add row or use existing amount display if present)
    - No UI text comparing selected vs effective tier
  </behavior>
  <action>
    Add effectivePayment state (tierName, rewards, amountEur) initialized null.

    In handleAutoConfirm and handleManualConfirm success branches (data.success && data.data?.confirmed):
    - Call setEffectivePayment({ tierName: data.data.tierName, rewards: data.data.rewards ?? [], amountEur: data.data.amountEur })
    - Then setConfirmed(true)

    In confirmed branch JSX:
    - Replace result.tierName with effectivePayment?.tierName ?? result.tierName (fallback only if API omitted fields — should not happen when amount known)
    - Replace result.rewards.map with (effectivePayment?.rewards ?? result.rewards).map
    - Add amount row when effectivePayment?.amountEur is defined using t("register.confirmAmount") or equivalent existing locale key

    Do NOT add upgrade callout when effective tier exceeds selected (D-10).
    Do NOT change pre-payment widget section — it may still show registration tier as payment target (deferred).
  </action>
  <acceptance_criteria>
    - effectivePayment state set on both auto and manual confirm success paths
    - Confirmed view uses effectivePayment.tierName and effectivePayment.rewards
    - No "upgrade" or "selected tier" comparison strings added
    - handleAutoConfirm still passes amount to API unchanged
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Track A post-payment UI reflects confirm-payment effective tier and cumulative rewards</done>
</task>

<task type="auto">
  <name>Task 2: FundraiserConfirmation effective tier parity (D-10, D-08)</name>
  <files>src/components/ui/FundraiserConfirmation.tsx</files>
  <read_first>
    - src/components/ui/FundraiserConfirmation.tsx (handleAutoConfirm, handleManualConfirm, confirmed banner ~lines 300-305)
    - src/components/ui/ConfirmationPanel.tsx (after Task 1 — mirror effectivePayment pattern)
    - src/components/ui/FundraiserConfirmation.module.css (confirmedBanner, summary styles if reusing)
  </read_first>
  <action>
    Add same effectivePayment state shape and setEffectivePayment calls in handleAutoConfirm and handleManualConfirm success branches.

    Expand confirmed branch beyond minimal banner:
    - Show effective tier name (effectivePayment.tierName)
    - Show cumulative rewards list (effectivePayment.rewards) using same list markup pattern as ConfirmationPanel or existing FundraiserConfirmation styles
    - Show amountEur when present

    Reuse ConfirmationPanel.module.css patterns only if FundraiserConfirmation already imports its own module — prefer FundraiserConfirmation.module.css classes for consistency.

    Pre-payment section unchanged; gate props from Plan 02 remain.
  </action>
  <acceptance_criteria>
    - FundraiserConfirmation sets effectivePayment on confirm success
    - Confirmed state displays tier name and rewards from API, not registration.tierName
    - Track B confirmed UX includes tier + rewards summary comparable to Track A
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && grep -c setEffectivePayment src/components/ui/FundraiserConfirmation.tsx | grep -qv '^0$'</automated>
  </verify>
  <done>Track B post-payment UI shows effective tier and rewards from confirm-payment response</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConfirmationPanel component test — effective tier overrides registration (D-10)</name>
  <files>src/__tests__/ConfirmationPanel.test.tsx</files>
  <read_first>
    - src/__tests__/RegisterClient.test.tsx (fetch mock pattern)
    - src/components/ui/ConfirmationPanel.tsx (after Tasks 1-2)
    - src/components/ui/registerTypes.ts (RegisterResponse mock shape)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-VALIDATION.md (task 06.1-03-01)
  </read_first>
  <behavior>
    - Mock registration result: tierName "Sprinter", rewards ["Running", "Sticker pack"], amountEur 15
    - Mock confirm-payment response: tierName "Ultramarathoner", rewards merged list, amountEur 100, confirmed true
    - After confirm flow completes, confirmed view text contains "Ultramarathoner" not "Sprinter"
    - Confirmed rewards list includes ultramarathoner-tier reward string from mock
  </behavior>
  <action>
    Create src/__tests__/ConfirmationPanel.test.tsx.

    Mock fetch for /api/register/confirm-payment returning effective tier different from registration.

    Render ConfirmationPanel with mock RegisterResponse; trigger payment success or call confirm handler via exposed flow (simulate onPaymentSuccess callback from WhyDonateWidget mock or invoke handleAutoConfirm if testable).

    Assert confirmed DOM shows API tierName and does not show registration tierName as the displayed tier label.

    Keep test focused — mock WhyDonateWidget if import complexity requires vi.mock("@/components/ui/WhyDonateWidget").
  </action>
  <acceptance_criteria>
    - ConfirmationPanel.test.tsx exists with at least one test for effective tier display
    - Test proves €100 payment after €15 Sprinter registration shows Ultramarathoner (or mock equivalent)
    - npx vitest run src/__tests__/ConfirmationPanel.test.tsx -x passes
    - npm test full suite passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/ConfirmationPanel.test.tsx -x && npm test</automated>
  </verify>
  <done>Automated test verifies post-payment UI uses API effective tier, not registration selection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirm-payment API → ConfirmationPanel state | Client trusts API tier/rewards for display; must match email (D-11) |
| Registration result → pre-payment UI | Selected tier shown as payment target only; not authoritative for rewards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-09 | Tampering | Stale registration tier in UI | mitigate | effectivePayment state sourced only from confirm-payment success response |
| T-06.1-10 | Information Disclosure | Upgrade callout leaks tier policy | accept | D-10 explicitly forbids comparison UI |
| T-06.1-11 | Repudiation | UI/email tier mismatch | mitigate | Both consume same confirmPayment return from Plan 01 |
| T-06.1-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run src/__tests__/ConfirmationPanel.test.tsx -x` green
- `npm test` full suite green
- `npm run build` succeeds (static export)
- Manual: overpay scenario shows higher effective tier rewards on confirmed panel
</verification>

<success_criteria>
- Post-payment UI on Track A and Track B shows effective tier + cumulative rewards from API (D-10, D-12)
- No selected-vs-effective comparison in normal UX
- Component test covers registration vs effective tier divergence
- Build and typecheck pass
</success_criteria>

<output>
Create `.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-03-SUMMARY.md` when done
</output>
