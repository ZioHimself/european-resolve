---
phase: 06.1-tier-amount-enforcement-effective-tier-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 06.1-01
files_modified:
  - src/components/ui/WhyDonateWidget.tsx
  - src/components/ui/ConfirmationPanel.tsx
  - src/components/ui/FundraiserConfirmation.tsx
  - src/__tests__/WhyDonateWidget.test.tsx
autonomous: true
requirements:
  - REGA-06
must_haves:
  truths:
    - "Registration widget step-1 Next is blocked when base amount is below selected tier minimum (D-01)"
    - "Inline error appears in #donation-amount-error-{id} with D-03 copy when gate fires"
    - "Gate error is fix-amount-only with no tier downgrade links or tier picker on payment step (D-02)"
    - "Amount field pre-fills with tier price and sessionStorage is written immediately on load (D-06, D-09)"
    - "Pre-fill and gate props wired identically in Track A ConfirmationPanel and Track B FundraiserConfirmation (D-08)"
    - "Gate error and pre-payment copy state minimum only with no overpay upgrade hints (D-07)"
    - "FundraiserPage visitor donate and donor wall widgets have no minimum gate (D-04, D-05)"
    - "Gate compares base donation amount only, not tip (RESEARCH pitfall 2)"
  artifacts:
    - path: src/components/ui/WhyDonateWidget.tsx
      provides: minTierAmount/minTierName props, capture-phase gate, tier price pre-fill
      contains: "wd-part-step-one-next"
    - path: src/components/ui/ConfirmationPanel.tsx
      provides: passes minTierAmount and minTierName to widget
    - path: src/components/ui/FundraiserConfirmation.tsx
      provides: passes minTierAmount and minTierName to widget
  key_links:
    - from: src/components/ui/ConfirmationPanel.tsx
      to: src/components/ui/WhyDonateWidget.tsx
      via: minTierAmount={result.amountEur} minTierName={result.tierName}
      pattern: "minTierAmount=\\{result\\.amountEur\\}"
    - from: src/components/ui/FundraiserPage.tsx
      to: src/components/ui/WhyDonateWidget.tsx
      via: widget embed WITHOUT minTier props (contrast — do not modify FundraiserPage)
      pattern: "WhyDonateWidget"
---

<objective>
Add registration-only step-1 minimum gate and tier-price pre-fill to WhyDonateWidget; wire gate props from Track A and Track B confirmation panels only.

Purpose: Block casual underpayment before Stripe per ADR-009 and D-01 through D-09.
Output: Gate via capture-phase listener on `.wd-part-step-one-next`; pre-fill `other-amount-number-${id}`; extended widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-CONTEXT.md
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-RESEARCH.md
@.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-PATTERNS.md
@src/components/ui/WhyDonateWidget.tsx
@src/lib/whydonatePaymentRedirect.ts

<interfaces>
From WhyDonateWidget.tsx (extend):
```typescript
interface WhyDonateWidgetProps {
  shortcode: string;
  lang?: string;
  onPaymentSuccess?: (amount: number, details?: {...}) => void;
  onDetectionFailed?: () => void;
  donorInfo?: DonorInfo;
  donationStorageKeys?: DonationStorageKeys;
  /** Registration flows only — omit for visitor/donor-wall embeds (D-04) */
  minTierAmount?: number;
  minTierName?: string;
}
```

Shadow DOM contract (wp_styling.js):
- Amount input: `#other-amount-number-${id}` where id = `${shortcode}-1`
- Step-1 Next: `.wd-part-step-one-next`
- Error element: `#donation-amount-error-${id}`
- validateTotalAmount() is closure-scoped — NOT wrappable (use capture click per RESEARCH Option A)

D-03 error copy template: "Minimum donation for {tierName} is €{price}. Please enter at least this amount to continue."

Gate activates only when BOTH minTierAmount and minTierName are set (prop presence = scope switch).

#step-three-portal-${id} is document light DOM — OUT OF SCOPE; do not implement step-3 validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WhyDonateWidget gate + pre-fill + immediate sessionStorage (D-01–D-03, D-06, D-09)</name>
  <files>src/components/ui/WhyDonateWidget.tsx</files>
  <read_first>
    - src/components/ui/WhyDonateWidget.tsx (readAmount, setInputValue, handleShadowRoot, attachDonationPersistence, cleanup)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-RESEARCH.md (Hook Option A, pre-fill pattern)
    - src/lib/whydonatePaymentRedirect.ts (PAYMENT_AMOUNT_STORAGE_KEY = r4u:donation-amount)
  </read_first>
  <behavior>
    - When minTierAmount and minTierName both set: pre-fill other-amount-number with tier price on shadow root attach
    - sessionStorage amount key written immediately after pre-fill (not waiting for user input)
    - Capture-phase click on .wd-part-step-one-next: if readAmount(shadow, id) < minTierAmount, preventDefault + stopImmediatePropagation, show D-03 error in #donation-amount-error-${id}
    - When amount >= minTierAmount: clear/hide error element, allow native WhyDonate handler
    - Gate uses readAmount (base only) not tip-inclusive total
    - When minTierAmount omitted: no gate listener attached
  </behavior>
  <action>
    Add optional minTierAmount?: number and minTierName?: string to WhyDonateWidgetProps; destructure in component.

    Implement attachTierAmountGate(shadow, id, minAmount, tierName): () => void per RESEARCH — capture listener on .wd-part-step-one-next, compare readAmount to minAmount, write D-03 copy to #donation-amount-error-${id}.

    In handleShadowRoot, when minTierAmount is defined and > 0:
    1. setInputValue on #other-amount-number-${id} with String(minTierAmount)
    2. If donationStorageKeys present, call persistDonationDetails immediately (D-09)
    3. Attach gate via attachTierAmountGate; store detach in ref; retry attachment in MutationObserver callback when button not yet present (pitfall 1)

    Gate listener cleanup on unmount alongside detachPersistence.

    Do NOT wrap validateTotalAmount — closure-scoped per locked decision.
    Do NOT add overpay hint copy (D-07).
  </action>
  <acceptance_criteria>
    - WhyDonateWidgetProps includes minTierAmount and minTierName optional fields
    - attachTierAmountGate uses addEventListener(..., true) capture phase
    - Pre-fill runs when minTierAmount set; persistDonationDetails called immediately after
    - Error copy matches D-03 template with interpolated tierName and price
    - No gate code path when minTierAmount is undefined
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Widget implements capture-phase step-1 gate and tier-price pre-fill with immediate sessionStorage</done>
</task>

<task type="auto">
  <name>Task 2: Wire registration confirmation panels with gate props (D-04, D-08)</name>
  <files>src/components/ui/ConfirmationPanel.tsx, src/components/ui/FundraiserConfirmation.tsx</files>
  <read_first>
    - src/components/ui/ConfirmationPanel.tsx (WhyDonateWidget embed ~lines 275-286; invoice widget ~179-182 without gate)
    - src/components/ui/FundraiserConfirmation.tsx (WhyDonateWidget embed ~lines 259-271)
    - src/components/ui/FundraiserPage.tsx (lines 416-422 — visitor embed omits minTier; do NOT modify)
  </read_first>
  <action>
    ConfirmationPanel.tsx — on the pre-payment WhyDonateWidget embed (not the invoice/collapsed widget):
    - Add minTierAmount={result.amountEur}
    - Add minTierName={result.tierName}

    FundraiserConfirmation.tsx — on the payment WhyDonateWidget embed:
    - Add minTierAmount={registration.amountEur}
    - Add minTierName={registration.tierName}

    Verify invoice-only WhyDonateWidget in ConfirmationPanel (invoiceOpen branch) remains without minTier props.

    Do NOT modify FundraiserPage.tsx visitor donate or donor wall embeds — they must remain unrestricted (D-05).
  </action>
  <acceptance_criteria>
    - ConfirmationPanel payment widget passes minTierAmount and minTierName from result
    - FundraiserConfirmation payment widget passes minTierAmount and minTierName from registration
    - ConfirmationPanel invoice widget has no minTier props
    - FundraiserPage.tsx unchanged (grep shows no minTierAmount in that file)
    - npm run typecheck passes
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && grep -l minTierAmount src/components/ui/ConfirmationPanel.tsx src/components/ui/FundraiserConfirmation.tsx | wc -l | grep -q 2</automated>
  </verify>
  <done>Track A and Track B registration payment widgets pass gate props; visitor/donor-wall embeds untouched</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend WhyDonateWidget tests for gate, pre-fill, and no-gate scoping</name>
  <files>src/__tests__/WhyDonateWidget.test.tsx</files>
  <read_first>
    - src/__tests__/WhyDonateWidget.test.tsx (simulateWidgetInit harness, sessionStorage tests)
    - .planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-VALIDATION.md (task 06.1-02-01 through 06.1-02-03)
  </read_first>
  <behavior>
    - Mock shadow DOM includes .wd-part-step-one-next button and #donation-amount-error-${id}
    - With minTierAmount=30: after init, sessionStorage amount is "30"
    - Click Next with amount 20: error element visible with "Minimum donation" text; step advance blocked (onclick spy not called if mocked)
    - Click Next with amount 30: error hidden; advance allowed
    - Without minTierAmount prop: no error on low amount click
  </behavior>
  <action>
    Extend simulateWidgetInit to include step-one Next button and donation-amount-error div.

    Add test cases per behavior block. Use vi.fn() onclick on mock button to verify stopImmediatePropagation prevents handler when below minimum.

    Run full widget test file — all existing Phase 6 tests must remain green.
  </action>
  <acceptance_criteria>
    - At least 3 new test cases: pre-fill+stash, gate blocks below min, no gate when props omitted
    - npx vitest run src/__tests__/WhyDonateWidget.test.tsx -x passes with 0 failures
    - No regressions in existing payment detection tests
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/WhyDonateWidget.test.tsx -x</automated>
  </verify>
  <done>Widget gate and pre-fill behaviors covered by automated tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser shadow DOM → WhyDonate widget | Client-side UX gate; not a security boundary |
| Registration panel → WhyDonateWidget props | minTierAmount scopes gate to registration flows only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-05 | Tampering | Devtools gate bypass | transfer | Backend Plan 01 getEffectiveTier handles downgrade (D-13) |
| T-06.1-06 | Tampering | Gate applied to donor wall | mitigate | Prop scoping — only ConfirmationPanel + FundraiserConfirmation pass minTier (D-04/D-05) |
| T-06.1-07 | Spoofing | Wrong minTierAmount prop | accept | Prop sourced from registration API response; same trust as registration tier |
| T-06.1-08 | Denial of Service | WhyDonate DOM contract break | accept | MutationObserver retry; manual QA on wp_styling.js updates |
| T-06.1-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run src/__tests__/WhyDonateWidget.test.tsx -x` green
- `npm run typecheck` green
- grep confirms FundraiserPage has no minTierAmount
</verification>

<success_criteria>
- Step-1 gate blocks under-minimum amounts with inline shadow DOM error (D-01, D-03)
- Tier price pre-filled and stashed immediately (D-06, D-09)
- Gate only on registration confirmation widgets (D-04, D-05, D-08)
- All widget tests pass
</success_criteria>

<output>
Create `.planning/phases/06.1-tier-amount-enforcement-effective-tier-policy/06.1-02-SUMMARY.md` when done
</output>
