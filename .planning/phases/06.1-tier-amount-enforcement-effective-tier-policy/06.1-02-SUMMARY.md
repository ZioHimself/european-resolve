---
phase: 06.1-tier-amount-enforcement-effective-tier-policy
plan: 02
subsystem: ui
tags: [whydonate, widget, gate, sessionStorage]

requires:
  - phase: 06.1-01
    provides: backend downgrade path when gate bypassed (D-13)
provides:
  - Registration-only step-1 minimum gate on WhyDonateWidget
  - Tier price pre-fill and immediate sessionStorage write
  - Gate props wired from ConfirmationPanel and FundraiserConfirmation only
affects: [06.1-03]

tech-stack:
  added: []
  patterns:
    - "Capture-phase click listener on .wd-part-step-one-next"
    - "minTierAmount/minTierName prop scoping for registration embeds only"

key-files:
  created: []
  modified:
    - src/components/ui/WhyDonateWidget.tsx
    - src/components/ui/ConfirmationPanel.tsx
    - src/components/ui/FundraiserConfirmation.tsx
    - src/__tests__/WhyDonateWidget.test.tsx

key-decisions:
  - "Gate activates only when both minTierAmount and minTierName are set"
  - "D-03 error copy written to #donation-amount-error-{id} in shadow DOM"
  - "FundraiserPage visitor/donor-wall embeds unchanged (no minTier props)"

requirements-completed: [REGA-06]

completed: 2026-08-06
---

# Phase 06.1 Plan 02 Summary

**Registration payment widgets block step-1 advance below tier minimum, pre-fill tier price, and stash amount immediately.**

## Accomplishments

- Extended `WhyDonateWidget` with optional `minTierAmount` / `minTierName` props
- Implemented capture-phase gate on `.wd-part-step-one-next` using base amount only
- Pre-fills `#other-amount-number-{id}` and writes sessionStorage on attach
- Wired gate props from Track A and Track B confirmation panels; invoice widget and FundraiserPage untouched
- Added 4 widget tests for pre-fill, gate block/allow, and no-gate scoping

## Self-Check: PASSED

- `npx vitest run src/__tests__/WhyDonateWidget.test.tsx` — green
- `npm run typecheck` — green
